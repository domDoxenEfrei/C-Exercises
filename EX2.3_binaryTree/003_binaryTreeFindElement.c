/*
Code a function int tree_has_value(Node* tree, int val) taking as input the root of a binary tree and an integer and returning 1 if the tree contains val or 0 if the tree does not contain the value. 

*/

